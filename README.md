# drift-sentry
Catch silent ML model failures your uptime dashboard never shows: PSI drift monitors + output-quality SLOs, with measured time-to-detect for data drift, concept drift, and pipeline bugs.
